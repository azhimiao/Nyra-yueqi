/** User-facing copy for External Required (Explore / Assistant). */

import { getExternalRequiredMessage } from "../onboarding/errors.js";

export const EXTERNAL_REQUIRED_USER_MESSAGE = getExternalRequiredMessage();
